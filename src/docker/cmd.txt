✅ Executar e subir uma imagem via docker
----------------------------------------
✅ Criar uma network ou seja uma comunicação com outro container

docker network create library-network   
----------------------------------------
-e enviroment
--name namo do banco
--p porta que vai rodar no docker
postgres: version
-d rodar em background, pode fechar o terminal

✅ postgres
docker run --name librarydb -p 5432:5432 -e POSTGRES_PASSWORD=postgres -e POSTGRES_USER=postgres -e POSTGRES_DB=library -d postgres:16.3

✅ Criar com network
docker run --name librarydb -p 5432:5432 -e POSTGRES_PASSWORD=postgres -e POSTGRES_USER=postgres -e POSTGRES_DB=library -d --network library-network postgres:16.3

✅ dpage/pgadmin4
docker run --name pgadmin4 -p 15432:80 -e PGADMIN_DEFAULT_EMAIL=admin@admin.com -e PGADMIN_DEFAULT_PASSWORD=admin dpage/pgadmin4

-----------------------------------------
✅ Baixar última versão 
docker pull postgres 16x

----------------------------------------
✅ lista as imagens rodando
docker ps 

----------------------------------------
✅ Parar um container docker
docker stop pgadmin4 -> aqui vai o nome da imagem -< 

----------------------------------------
✅ Lista todas as imagens docker ativas 

docker ps -a

----------------------------------------
✅ remover a imagen docker instalada/configurada 

docker container rm librarydb -> aqui vai o nome da imagem -<

----------------------------------------
✅ Verifique logs do container
Se ainda não funcionar, envie o resultado de:

docker logs librarydb

----------------------------------------
✅
Porta do Portgres

http://localhost:15432/browser/